name: reto_habitos
description: Proyecto de aplicacion sobre habitos
publish_to: none

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  firebase_core: 2.17.0
  firebase_auth: 4.7.0
  cloud_firestore: 4.8.3
  cupertino_icons: 1.0.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: 2.0.0

flutter:
  uses-material-design: true